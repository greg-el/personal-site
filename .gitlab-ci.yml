image: node:14.13.1

pages:
  cache:
    paths:
      - node_modules/

  stage: deploy
  variables:
    REACT_APP_GA_ID: $GA_ID
  script:
    - npm install
    - npm run build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public

  only:
    - deploy
